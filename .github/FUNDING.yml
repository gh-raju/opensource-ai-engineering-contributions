# Sponsorship configuration. GitHub renders a "Sponsor" button from the entries below.
# Replace the custom URL with your GitHub Sponsors handle once enabled (e.g. `github: gh-raju`)
# or a dedicated sponsorship/contact page.
custom: ["https://github.com/gh-raju"]
